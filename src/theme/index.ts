import {
  colors,
  media,
  fontSize,
  fontFamily,
  borderRadius,
  spacing,
  border,
} from "./declarations";

const theme = {
  colors,
  media,
  fontSize,
  fontFamily,
  borderRadius,
  spacing,
  border,
};

export default theme;
