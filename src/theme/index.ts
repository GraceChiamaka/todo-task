import {
  colors,
  media,
  fontSize,
  borderRadius,
  border,
  spacing,
  fontFamily,
} from "./declarations";

const theme = {
  colors,
  media,
  fontSize,
  borderRadius,
  spacing,
  border,
  fontFamily,
};

export default theme;
